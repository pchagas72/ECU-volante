#include "can_driver.h"
#include "dashboard.h" // Needs to access the shared data struct
#include "esp_log.h"

void can_driver_start(void) {
    ESP_LOGI("CAN_DRIVER", "CAN driver module started (not yet implemented).");
}
